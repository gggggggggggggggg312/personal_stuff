name: Test Workflow

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "It works!"

      - name: Show runner OS
        run: echo "Running on ${{ runner.os }}"


